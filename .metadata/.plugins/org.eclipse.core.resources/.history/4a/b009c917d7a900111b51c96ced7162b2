package com.expenses.portal.repository;

import java.util.Optional;

import org.springframework.data.jpa.repository.JpaRepository;

import com.expenses.portal.entity.LoginEntity;

public interface LoginRepository extends JpaRepository<LoginEntity, Long> {
	  Optional<LoginEntity> findByLoginEntityname(String LoginEntityname);

	    Optional<LoginEntity> findByEmail(String email);

	    Optional<LoginEntity> findByLoginEntitynameOrEmail(String LoginEntityname, String email);

	    boolean existsByLoginEntityname(String LoginEntityname);

	    boolean existsByEmail(String email);
}
